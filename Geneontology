class Graph:

    # Constructor
    def __init__(s):

        # default dictionary to store graph
        s.graph = defaultdict(list)

    # function to add an edge to graph
    def addEdge(a, u, v):
        s.graph[u].append(v)
